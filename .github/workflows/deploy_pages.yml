# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  # Runs on pushes targeting the default branch
  push:
    branches: [main]

  pull_request:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Allow one concurrent deployment
concurrency:
  group: pages
  cancel-in-progress: true

env:
  FILE: 'slides.py'  # Source file where scenes are defined
  MANIM: 'manim'  # manim or manimgl - which Manim renderer to use
  SCENES: 'Slide1_TitleIntroduction Slide2_CommunicationRules Slide3_CourseSummary Slide4_PromptEngineering Slide5_PromptComponents Slide6_ComponentsExample Slide7_PromptBestPractices Slide8_ContentCreationPrompts Slide9_ChatGPTInternetAccess Slide10_PartTransition Slide11_WooclapPlaceholder Slide12_LLMDefinition Slide13_HistoricalTimeline Slide14_RNNIntroduction Slide15_RNNSequential Slide16_TimelineContinued Slide17_UseCases Slide18_ToolsArchitectureTitle Slide19_NLPSteps Slide20_Word2VecIntroduction Slide21_TokensDefinition Slide22_EmbeddingsDefinition Slide23_EmbeddingsGeneration Slide24_Word2VecTraditional Slide25_Word2VecMethods Slide26_TransformerEmbeddings Slide27_BatDisambiguation Slide28_AttentionIntroduction Slide29_MultiHeadAttention Slide30_AttentionMechanismDeep Slide31_GroupedQueryAttention Slide32_TextGenerationTitle Slide33_ProbabilityBasedGeneration Slide34_EmbeddingPositionalEncoding Slide35_QueryKeyValue Slide40_ArchitectureSummary Slide41_PredictionLayer Slide42_TransformerArchitecture Slide44_ChallengesTitle Slide45_ChallengesOverview Slide46_Hallucinations Slide47_QuantizationQuestion Slide51_CostsAnalysis Slide52_PromptInjection Slide53_RLHF Slide56_TemperatureParameter Slide57_TopPSampling Slide58_TopKSampling Slide59_SystemPrompt Slide60_HuggingFaceIntro Slide61_DemoPlaceholder Slide62_ConclusionTitle Slide63_QuestionsAndFeedback Slide64_NextDates Slide65_ThankYou'  # Space-separated list of scenes to render
  USES_TEX: true  # true or false - disabling this will make the action run faster
  DISPLAY: :99  # Do not touch this

jobs:
  # Single deploy job since we're just deploying
  deploy:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Install Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: pip

    - name: Install manim dependencies on Ubuntu
      if: ${{ env.MANIM == 'manim' }}
      run: |
        sudo apt-get update
        sudo apt-get install libcairo2-dev libpango1.0-dev ffmpeg freeglut3-dev

    - name: Install manimgl dependencies on Ubuntu
      if: ${{ env.MANIM == 'manimgl' }}
      run: |
        sudo apt-get update
        sudo apt-get install libpango1.0-dev ffmpeg freeglut3-dev xvfb
        nohup Xvfb $DISPLAY &

    - name: Setup TeX Live
      if: ${{ env.USES_TEX == 'true' }}
      uses: zauguin/install-texlive@v4
      with:
        packages: >-
          amsmath
          babel-english
          cbfonts-fd
          cm-super
          count1to
          ctex
          doublestroke
          dvisvgm
          everysel
          fontspec
          frcursive
          fundus-calligra
          gnu-freefont
          jknapltx
          latex-bin
          mathastext
          microtype
          multitoc
          physics
          prelim2e
          preview
          ragged2e
          relsize
          rsfs
          setspace
          standalone
          tipa
          wasy
          wasysym
          xcolor
          xetex
          xkeyval

    - name: Install Python dependencies
      run: pip install -r requirements.txt

    - name: Build HTML
      run: |
        ${{ env.MANIM }} ${{ env.FILE }} ${{ env.SCENES }}
        manim-slides convert ${{ env.SCENES }} _site/index.html

    - name: Push to gh-pages branch
      if: github.event_name != 'pull_request'
      uses: s0/git-publish-subdir-action@develop
      env:
        REPO: self
        BRANCH: gh-pages
        FOLDER: _site
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
